# Copy to android/keystore.properties and fill in.
# NEVER commit the real file or the .keystore itself.
storeFile=../../neoncell-release.keystore
storePassword=CHANGE_ME
keyAlias=neoncell
keyPassword=CHANGE_ME
